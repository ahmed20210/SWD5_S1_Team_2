@{
    ViewData["Title"] = "Verification Successful";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow text-center">
                <div class="card-body p-5">
                    <div class="mb-4">
                        <i class="fas fa-check-circle text-success" style="font-size: 5rem;"></i>
                    </div>
                    <h2 class="mb-4">Verification Successful!</h2>
                    <p class="mb-4">Your email has been successfully verified.</p>
                    <div>
                        <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Continue to Homepage</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
